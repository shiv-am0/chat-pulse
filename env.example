# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Database Configuration (Docker Compose)
MONGO_URI=mongodb://admin:password@localhost:27017/chat-app?authSource=admin

# Redis Configuration (Docker Compose)
REDIS_URL=redis://localhost:6379

# Kafka Configuration (Docker Compose)
KAFKA_BROKER=localhost:9092

# Optional: JWT Secret (if you plan to add authentication later)
JWT_SECRET=qwerty

# Optional: For production, use these values instead:
# MONGO_URI=mongodb://your-mongo-host:27017/chat-app
# REDIS_URL=redis://your-redis-host:6379
# KAFKA_BROKER=your-kafka-host:9092 